modelwatch:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "../../data/modelwatch.db"
      schema: main
      schemas_and_paths:
        main: "../../data/modelwatch.db"
